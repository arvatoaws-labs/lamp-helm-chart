apiVersion: v1
kind: Service
metadata:
  name: {{ include "lamp-helm-chart.fullname" . }}
  labels:
    {{- include "lamp-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lamp-helm-chart.selectorLabels" . | nindent 4 }}
